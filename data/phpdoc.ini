;; phpDocumentor configuration for Fizzy
;;
;; Use with phpDocumentor command line interface from the project root:
;;  phpdoc -c data/phpdoc.ini

[Parse Data]
title = Fizzy API Documentation

;; parse files that start with a . like .bash_profile
;; legal values: true, false
hidden = false

;; show elements marked @access private in documentation by setting this to on
;; legal values: on, off
parseprivate = on

;; parse with javadoc-like description (first sentence is always the short description)
;; legal values: on, off
javadocdesc = off

;; add any custom @tags separated by commas here
;; legal values: any legal tagname separated by commas.
;customtags = mytag1,mytag2

;; This is only used by the XML:DocBook/peardoc2 converter
defaultcategoryname = Documentation

;; what is the main package?
;; legal values: alphanumeric string plus - and _
defaultpackagename = global

;; output any parsing information?  set to on for cron jobs
;; legal values: on
quiet = on

;; where should the documentation be written?
;; legal values: a legal path
target = data/docs

;; Which files should be parsed out as special documentation files, such as README,
;; INSTALL and CHANGELOG?  This overrides the default files found in
;; phpDocumentor.ini (this file is not a user .ini file, but the global file)
readmeinstallchangelog = README, INSTALL, LICENSE

;; comma-separated list of directories to parse
;; legal values: directory paths separated by commas
directory = controllers,models,library

;; Ouput
output=HTML:frames:default

;; turn this option on if you want highlighted source code for every file
;; legal values: on/off
sourcecode = on