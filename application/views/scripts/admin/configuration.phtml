<h1>Configuration</h1>
<p>
    The configuration below is loaded when Fizzy starts. For an overview of the
    fizzy configuration settings see <a href="http://project.voidwalkers.nl/project/fizzy">the manual</a>. <br />
    These settings can be changed in the files found under 
    <span class="code-inline"><?= Fizzy_Config::getInstance()->getPath('configs') ?></span>.
</p>

<?php foreach($this->configuration as $section => $sectionData) : ?>

<h2><?= $section ; ?></h2>

<table>
    <thead>
        <tr>
            <td style="width: 250px;">Setting</td>
            <td>Value</td>
        </tr>
    </thead>
    <tbody>
        <?php
        $row = 0;
        foreach($sectionData as $setting => $value) : ?>
        <tr<?= ($row % 2 == 0) ? ' class="alt"' : ''; ?>>
            <td><?= $setting; ?></td>
            <td>
                <?php if(is_array($value)) :
                    echo implode(',<br />', $value);
                else :
                    echo $value;
                endif; ?>
            </td>
        </tr>
        <?php
        $row++;
        endforeach; ?>
    </tbody>
</table>
<br />
<?php endforeach; ?>


